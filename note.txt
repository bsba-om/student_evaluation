🧠 SMART LOGIC
1. Auto-detect grade anomalies (e.g., sudden drop >1.5 from previous semester GWA) and show a warning toast.
2. Predict graduation date based on current pace + remaining units; show ETA chip in GWA strip.

3. Smart prerequisite chain visualizer — show a mini-tree on hover for any locked subject.
4. GPA trend sparkline (last 4 semesters) in the GWA strip.
5. Auto-flag at-risk students (GWA > 2.75 OR 2+ failed subjects) with a red banner.

🔒 STRICTER VALIDATION
1. Block saving grades if the subject's prerequisite chain has unresolved conditionals (4.00).
2. Enforce retake semester rule server-side: subject can ONLY be retaken in the same sem it's offered.
4. Lock finalized semesters completely — no grade edits, even by admin, without audit log entry.
5. Prevent promotion if >3 failed subjects OR cumulative GWA < 3.0.
😊 USER-FRIENDLY UX
1. Keyboard shortcuts: Tab moves to next input, Ctrl+S saves all dirty grades, Esc closes modal.
2. Auto-save draft grades every 30s to localStorage (recover on accidental close).
3. Bulk grade entry mode: paste CSV (code,grade) to fill multiple subjects at once.
4. Breadcrumb navigation inside the overlay (Mentee → Year → Semester → Subject).
🎯 FLEXIBLE PROGRESSION
1. Allow partial promotion: student can carry up to 2 failed subjects forward if not prerequisites.
2. Summer class recommendation for retakes (auto-generate summer enrollment list).
3. Custom academic plan builder: let instructor override recommended subjects manually.
4. Multi-AY view: compare same student across past academic years side-by-side.
5. Conditional graduation check: show remaining requirements dynamically.
⚖️ CONDITIONAL RULES ENGINE
1. Configurable rule set (JSON) for: passing grade threshold, retake eligibility, promotion criteria.
2. Conditional probation status: auto-assign "Academic Warning" / "Probation" / "Dismissal" tiers.

Apply all changes while preserving the existing gold/cream theme, Poppins+Playfair typography, 
and the three-tab overlay layout. give full evaluation.php code
